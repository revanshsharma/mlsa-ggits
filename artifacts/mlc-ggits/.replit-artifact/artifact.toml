kind = "web"
previewPath = "/"
title = "Microsoft Campus Club GGITS"
version = "1.0.0"
id = "artifacts/mlc-ggits"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22200

[services.development]
run = "pnpm --filter @workspace/mlc-ggits run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mlc-ggits", "run", "build" ]
publicDir = "artifacts/mlc-ggits/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22200"
BASE_PATH = "/"
